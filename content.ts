export const content: QueryParams = {
  title: 'YouTube Cover Generator',
  subtitle: 'Focus on video production',
  authorName: 'Jakub Pusiak',
};

export const settings = {
  width: 650,
  height: 650,
  deviceScaleFactor: 2,
};

export type QueryParams = {
  title: string;
  subtitle: string;
  authorName: string;
}